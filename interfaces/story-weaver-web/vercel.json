{
  "buildCommand": "python -m pip install -r requirements.txt && next build",
  "installCommand": "pip install -r requirements.txt && npm install",
  "framework": "nextjs",
  "builds": [
    {
      "src": "api/**/*.py",
      "use": "@vercel/python"
    }
  ]
  
} 